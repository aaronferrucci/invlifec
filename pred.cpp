#include "succ.h"
#include "pred.h"
#include <stdio.h>

Pred::Pred()
{
  valid = false;
}

// given successor s, return the first predecessor state, or null
// if none found.
Pred::invert(Succ s)
{
}

// given successor s, return the first predecessor start after 'after'
// in the search sequence, or null if none found.
Pred::invert(Succ s, Pred after)
{
}

